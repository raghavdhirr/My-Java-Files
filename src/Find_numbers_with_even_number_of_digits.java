import java.util.Scanner;public class Find_numbers_with_even_number_of_digits {    public static void main(String[] args) {    Scanner sc = new Scanner(System.in);    System.out.print("Enter the size of your array: ");    int size = sc.nextInt();    System.out.print("Enter " + size + " elements: ");    int [] array = new int[size];    //Input array elements        for (int i = 0; i < array.length; i++) {            array[i]= sc.nextInt();        }        System.out.print("Number of elements with even digit of numbers: "+ findNumbers(array));    }    //finding elements of even digits    static int findNumbers(int[] nums) {        int count = 0;        for (int i = 0; i < nums.length; i++) {            int num = nums[i];            if (Checkeven(num)) {                count++;            }        }        return count;    }    //Checking if the element is even or not using Method2    static boolean Checkeven(int num) {        int NumberOfDigits = digitsMethod2(num);        if (NumberOfDigits % 2 == 0){            return true;        }        else return false;    }   //Checking number of digits in each element    static int digits(int num) {        int count = 0;        while(num>0) {            count++;            num= num/10;        }        return count;    }    //New method to find digits in each element    static int digitsMethod2(int number) {        if(number<0) {            number*= -1;        }        return (int)(Math.log10(number)) +1;    }}