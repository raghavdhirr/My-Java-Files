import java.util.Scanner;public class ceiling_of_a_number_problem {    public static void main(String[] args) {        //Problem is that find the index of smallest element that is greater than or equal to the target element        Scanner sc = new Scanner(System.in);        System.out.print("Enter the size of your array: ");        int size = sc.nextInt();        System.out.print("Enter " + size + " elements in your array: ");        int[] arr = new int[size];        for (int i = 0; i < arr.length; i++) {            arr[i]= sc.nextInt();        }        System.out.print("Enter the target element: ");        int target= sc.nextInt();        int ans = ceiling_problem(arr, target);        System.out.print("Index of smallest number that is greater than or equal to "+ target + " is: "+ ans);    }    static int ceiling_problem(int[]arr, int target) {        if(target > arr[arr.length-1]) {            return -1;        }        int start = 0;        int end = arr.length-1;        while (start <= end) {            int mid = (start + ((end - start) / 2));            if (target < arr[mid]) {                end = mid -1;            } else if (target > arr[mid]) {                start = mid + 1;            } else {                return mid;            }        }        return start;    }}