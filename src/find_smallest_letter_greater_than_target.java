import java.util.Scanner;public class find_smallest_letter_greater_than_target {    public static void main(String[] args) {        //https://leetcode.com/problems/find-smallest-letter-greater-than-target/        Scanner sc = new Scanner(System.in);        System.out.print("Enter the size of your array: ");        int size = sc.nextInt();        System.out.print("Enter " + size + " elements in your array: ");        int[] letter = new int[size];        for (int i = 0; i < letter.length; i++) {            letter[i]= sc.nextInt();        }        System.out.print("Enter the target element: ");        int target= sc.nextInt();        int ans = nextGreatestLetter(letter, target);        System.out.print("Index of smallest letter that is greater than or equal to "+ target + " is: "+ ans);    }    public char nextGreatestLetter(char[] letters, char target) {        int start = 0;        int end = letters.length-1;        while (start <= end) {            int mid = (start + ((end - start) / 2));            if (target < letters[mid]) {                end = mid -1;            } else {                start = mid + 1;            }        }        return letters[start % letters.length];    }}